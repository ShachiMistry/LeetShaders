# Client-exposed config (safe to ship to the browser).
# Populated by Content & Backend when the Supabase project exists.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only. Never prefix with VITE_.
# Consumed by the Vercel/Supabase Edge Function that proxies Anthropic calls.
ANTHROPIC_API_KEY=
